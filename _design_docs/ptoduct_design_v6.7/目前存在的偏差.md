序列场景模块：
目前存在的问题：
1.存在节拍数量不到位的现象。一个资产文件内会描述清楚每一份场景的描述，但是节拍从最后一个场景开始的号码开始编写节拍，也就是会有1-6的场景，但是节拍是从6-1开始写的。但是这个现象不是高频率的
2.平均会有失败两次才能稳定输出场景节拍文件。

写作模块
3.写作环节短剧情景下，资产文件的单位的最小单位上一个序列，序列下有8-15集。总共的资产文件的数量就是上游场景节拍输出的资产文件的个数，
目前现状：对同一份正式剧本资产文件进行反复的覆写，在原文上覆盖新的信息，删除原有的。

场景节拍模块
4.场景节拍输出支持并行输出，指同时输出多个序列为单位的场景节拍文件。

调度模块
5.工具调用量目前完全不够用，如果要写序列，短剧可能会有20多个序列，完全超出单次对话的总任务量。

