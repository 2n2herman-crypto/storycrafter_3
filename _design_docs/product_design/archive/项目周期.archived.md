# 🗄 已归档 — 项目周期（初版）

> **归档日期**：2026-06-25  
> **原因**：已被产品设计决策覆盖，不再作为开发参考。  
> **替代**：当前 Phase 1a 范围以 `开发规划.md`、`技术开发文档.md`、`状态机.md` 等最新设计文档为准。

---

phase1：
目标:实现多agent调度的功能
需要实现：
1.简单版Orchestrator调度中枢，能够基本理解啥时候用大纲，啥时候用细纲
2.大纲幕序列agent+场景节拍agent，双agent调度输出完、
{其中，只需要完成事件流的串接即可}
3.调度完，输出一个分幕的大的大纲单，不做任何拆分
4.直接只交付一个文件
5.提供一个前端

代办：
1.两个agent的system prompt (done)
2.上下游数据接口契约 (done)
3.状态机 (done)
4.主agent调度规则 (done)
5.技术栈：Electron 28+ / React 18 / TypeScript / Zustand / Vite / diff / mammoth / xlsx / chokidar
6.前端设计文档 (done: frontend/design-doc.md)
   - 6.1 UI 架构总览：三栏布局 + 底栏
   - 6.2 组件树与职责定义
   - 6.3 数据流模型：IPC通信 / Zustand状态管理 / 文件变更响应
   - 6.4 Diff 引擎设计：词级diff + Markdown渲染策略
   - 6.5 文件导入协议：mammoth(.docx) / xlsx(.xlsx) / 原生(.md)
   - 6.6 状态联动逻辑：Phase → 卡片可见性映射
7.前端开发
   - 7.1 项目初始化：Electron + Vite + React + TypeScript + electron-builder
   - 7.2 主进程实现：文件监听(chokidar)、IPC通道注册、文件读写、Word/Excel解析
   - 7.3 Preload 桥接层：contextBridge 暴露安全 API
   - 7.4 状态管理：Zustand store（assets / ui / phase / chat / import）
   - 7.5 三栏布局：可拖拽缩放的 BaselinePanel / CurrentPanel / AssetCardPanel
   - 7.6 Diff 引擎：词级 diff 计算 + <ins>/<del> 嵌入 + react-markdown 渲染
   - 7.7 底栏：Phase 下拉选择器 + 对话输入 + 拖拽文件导入 + 导入预览弹窗
   - 7.8 快照管理：确认按钮 → 生成 *.approved.md → 更新基线栏
   - 7.9 全局样式：暗色主题 + 滚动条 + diff 高亮配色



stage2：
1.在上述的基础上，在大纲幕序列后添加一个【情绪，信息披露单元】
2.情绪渲染单元主要负责完成情绪的标注【序列和场景维度，为了调度情绪，不必过于密集】
3.信息披露单元主要负责场景级别对观众的信息披露
4.技术需求：能够做到情绪和信息的协作和交互，并在综合采纳后交付至下游

stage3：
1.支线支持